#ifndef HEAP_C
#define HEAP_C

typedef struct pages
{
	unsigned int* address;
	char isFree;
	unsigned int size;
	
	pages_entries* next;
	
}pages_entries; 

extern unsigned int* __end;
unsigned int place_in_memory = __end;
unsigned int malloc(unsigned int size)
{
	
}
#endif
